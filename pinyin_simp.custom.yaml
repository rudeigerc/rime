patch:
  schema/dependencies/+:
    - melt_eng
  melt_eng:
    dictionary: melt_eng
    enable_completion: true
    enable_sentence: false
    initial_quality: "-1"
    spelling_hints: 9
  engine/translators/+:
    - table_translator@melt_eng
    - lua_translator@date_translator
  speller/alphabet: zyxwvutsrqponmlkjihgfedcbaZYXWVUTSRQPONMLKJIHGFEDCBA
  speller/algebra/+:
    # https://gist.github.com/lotem/2320943
    - derive/^([zcs])h/$1/             # zh, ch, sh => z, c, s
    - derive/^([zcs])([^h])/$1h$2/     # z, c, s => zh, ch, sh
    - derive/^n/l/                     # n => l
    - derive/^l/n/                     # l => n
    - derive/([ei])n$/$1ng/            # en => eng, in => ing
    - derive/([ei])ng$/$1n/            # eng => en, ing => in
__patch:
# Rx: emoji:customize:schema=pinyin_simp {
  - patch/+:
      __include: emoji_suggestion:/patch
# }
# Rx: BlindingDark/rime-lua-select-character:customize:schema=pinyin_simp {
  - patch/+:
      __include: lua_select_character:/patch
# }
# Rx: lotem/rime-octagram-data:customize:schema=pinyin_simp,model=hans {
  - patch/+:
      __include: grammar:/hans
# }
